类锁：
    static方法
    类.class

对象锁：
    sychronized(lock){}
    普通方法

区别：
    锁的范围
    生命周期
        类级别：
            Class文件是在jvm启动过程加载的，每个.class文件被加载后会产生Class对象，这个对象在jvm进程是全局唯一的
            通过static修饰的成员对象和方法都属于类级别，在类加载到内存而定义，在类被卸载而回收。
        对象级别：
            对象实例化而开始，对象回收（析构）而结束。

