内部类
    目的
        方便，简单，封装

    分类
        静态内部类（static）
        成员内部类
        局部内部类

        匿名内部类（特殊的局部内部类）
            语法
                new 父类名/父接口名

        局部内部类访问局部变量
        这个变量必须是fianl
        否则无法访问
        目的是延长变量的生命周期