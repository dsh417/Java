静态代理只能使用固定类型的对象，代码复用性维护性低。（问题）
动态代理实质上，是通过生成java文件，在编译后交给jvm运行，最后将新的对象返回（解决方式）

生成方法是 字节码重组
    通过反射实现了被代理对象的所有接口，通过拼接生成.java文件(因为涉及到大量字符串拼接，非常容易写错)

    通过java文件输出磁盘，进行编译，然后加载到虚拟机执行，最后返回实例化对象

    这就是JDK的动态代理

